
    # 3.1 구두점 복원 실행 (데이터 수집 후)
    logging.info("3단계: 구두점 복원 실행…")
    punct_path = project_root / "3_model" / "nlp" / "punctuation_restoration" / "main.py"
    punct_module = _load_module(punct_path, "main")
    punct_module()


    # 3.1 구두점 복원 실행 (데이터 수집 후)
    logging.info("3단계: 구두점 복원 실행…")
    punct_path = project_root / "3_model" / "nlp" / "punctuation_restoration" / "main.py"
    punct_module = _load_module(punct_path, "main")
    punct_module()

